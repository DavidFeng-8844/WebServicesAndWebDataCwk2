# Search Engine Tool: Video Demonstration Script

This document contains the 5-minute video demonstration script for the University Search Engine project. The script is designed to showcase technical mastery, algorithmic complexity, and professional software engineering practices.

| [Time] | [Visual Action] | [Audio Script] |
| :--- | :--- | :--- |
| **0:00 - 0:30** | **Terminal:** Open `src/main.py`. Type `load` and hit enter. Show the successful load message. Then type `build` briefly to show the crawler starting, then interrupt it (`Ctrl+C`). | "Hello. Today I'll demonstrate a high-performance web crawler and search engine, engineered from scratch in Python. Let's start the CLI. I'll run the 'load' command to instantly mount our pre-computed inverted index into memory. If we run 'build', you can see the crawler begins fetching. It's programmed to strictly enforce a 6-second politeness window between HTTP requests to avoid overwhelming the target server." |
| **0:30 - 1:15** | **Terminal:** Type `print nonsense`. Highlight the term statistics (DF, IDF). Then type `find indifference` and `find "good friends"`. | "Now, let's examine the index statistics using 'print'. For the term 'nonsense', the engine displays its Document Frequency and Inverse Document Frequency, showcasing our internal data transparency. Moving to retrieval: I'll search for 'indifference' and the multi-word query 'good friends'. The engine performs a ranked retrieval using an Okapi BM25 variant of the TF-IDF model. It scores documents based on term frequency, IDF, and document length normalization. Notice how our algorithm awards a custom adjacency bonus for phrase-like matches." |
| **1:15 - 2:00** | **Terminal:** Type `find "dumbledor"` (typo). Point to the *"Did you mean: dumbledore?"* output. Type `find " "` (empty). | "Let's test an edge case: a typo. I'll search for 'dumbledor'. Instantly, the system leverages an advanced Levenshtein distance algorithm to suggest: 'Did you mean: dumbledore?'. This query suggestion feature prevents zero-result dead ends, drastically improving user experience. Empty queries and non-existent words are also caught gracefully." |
| **2:00 - 2:45** | **VS Code:** Switch to `src/indexer.py`. Highlight the `InvertedIndex` class, specifically the `build` method's docstring showing the Big-O notations. | "Let's dive into the architecture. Here in the Indexer, a key algorithmic trade-off was choosing a nested dictionary structure. While it consumes slightly more memory than a flat list, it guarantees O(1) constant-time lookups for our vocabulary. Building the index operates at O(N * L) time complexity, where N is the number of pages and L is the average tokens per page." |
| **2:45 - 3:30** | **VS Code:** Switch to `src/search.py`. Highlight the strict type hints (`-> List[SearchResult]`) and the TF-IDF calculation math. | "Throughout the codebase, you'll see strict PEP-8 type hints and Google-style docstrings enforcing publication-quality standards. For ranking, the TF-IDF calculation is meticulously smoothed mathematically. This prevents fatal division-by-zero errors when a term exists in all documents, securing a mathematically stable, highly optimized ranking algorithm." |
| **3:30 - 4:00** | **Terminal:** Run `pytest -v tests/`. Let it pass instantly.<br><br>**Browser:** Show the GitHub Actions tab with green checkmarks. | "For quality assurance, I've implemented a professional test suite. I'll run `pytest` now. We achieve comprehensive coverage without making a single live HTTP request. How? By using a mocking strategy to intercept network calls and inject synthetic HTML payloads. Furthermore, this automated GitHub Actions CI/CD pipeline validates our code on every push." |
| **4:00 - 4:30** | **Terminal:** Run `git log --graph --oneline` or show the GitHub commit history tab. | "Looking at our Git history, you'll see a disciplined, iterative workflow. I utilized semantic commit messages—such as 'feat', 'fix', and 'docs'—to document the project's evolution clearly. Development was compartmentalized methodically, demonstrating industry-standard version control professionalism." |
| **4:30 - 5:00** | **VS Code / Presentation:** Show the `AI_DECLARATION_TEMPLATE.md` file, highlighting the "Where AI Hindered" section. | "Finally, regarding Generative AI: I utilized it as a pair-programming assistant. While excellent for scaffolding boilerplate, it demonstrated critical limitations in system architecture. For example, AI initially suggested a highly inefficient O(N) list-based inverted index. I had to reject this and manually architect the O(1) dictionary implementation. This reinforced my understanding of algorithmic data structures and proved that AI is a productivity multiplier, but no substitute for software engineering fundamentals." |
